<launch>
  <arg name="waypoint_file" default="$(find yeoms_control)/waypoints/test_straight_slow.csv" />
  <arg name="command_type" default="twist" />
  <arg name="record_bag" default="false" />
  <arg name="origin_lat" default="0.0" />
  <arg name="origin_lon" default="0.0" />
  <arg name="origin_alt" default="0.0" />
  <arg name="target_speed_override_mps" default="1.5" />
  <arg name="max_target_speed_mps" default="1.5" />

  <!-- UDP GPS -> /localization/ego_pose, /localization/ego_twist -->
  <include file="$(find yeoms_udp_bridge)/launch/udp_gps_localization.launch">
    <arg name="origin_lat" value="$(arg origin_lat)" />
    <arg name="origin_lon" value="$(arg origin_lon)" />
    <arg name="origin_alt" value="$(arg origin_alt)" />
  </include>

  <!-- Stanley -> /control/ctrl_cmd -->
  <include file="$(find yeoms_control)/launch/stanley_test.launch">
    <arg name="waypoint_file" value="$(arg waypoint_file)" />
    <arg name="command_type" value="$(arg command_type)" />
    <arg name="target_speed_override_mps" value="$(arg target_speed_override_mps)" />
    <arg name="max_target_speed_mps" value="$(arg max_target_speed_mps)" />
  </include>

  <!-- /control/ctrl_cmd -> MORAI Ego Ctrl Cmd UDP, target port 9093 -->
  <param name="/udp_bridge/ctrl_sender/max_speed_mps" value="$(arg max_target_speed_mps)" />
  <node pkg="yeoms_udp_bridge"
        type="udp_ctrl_cmd_sender.py"
        name="udp_ctrl_cmd_sender"
        output="screen" />

  <include if="$(arg record_bag)" file="$(find morai_competition_bringup)/launch/logging.launch" />
</launch>
