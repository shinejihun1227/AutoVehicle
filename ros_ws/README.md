# ROS Workspace

이 디렉토리는 ROS1 Noetic catkin workspace 구조를 저장소 안에서 관리하기 위한 skeleton이다.

실제 Ubuntu 환경에서는 보통 다음 경로에 workspace를 둔다.

```bash
~/morai_competition_ws
```

이 저장소의 `ros_ws/src` 아래 패키지 구조를 실제 catkin workspace의 `src`와 맞추는 방식으로 관리한다.

